query Series($host: String!, $seriesSlug: String!) {
  publication(host: $host) {
    series(slug: $seriesSlug) {
      id
      name
      description {
        text
      }
      posts(first: 1) {
        totalDocuments
      }
    }
  }
}
